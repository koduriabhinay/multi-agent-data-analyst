version: '3.8'
services:
  mada:
    build: .
    ports:
      - "8000:8000"
    environment:
      - LLM_PROVIDER=anthropic
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    command: python -m app.web_main
